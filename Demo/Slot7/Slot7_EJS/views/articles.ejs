<%- include('partials/header') %>
<ul>
  <% articles.forEach(article => { %>
  <li>
    <h2><%= article.title %></h2>
    <p>Tác giả: <%= article.author %></p>
    <p>Ngày đăng: <%= article.date %></p>
  </li>
  <% }) %>
</ul>
