services:
  - type: static
    name: homs-fe
    runtime: static
    buildCommand: npm run build
    publishPath: build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
